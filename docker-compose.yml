version: '3.8'

services:
  dashboard:
    image: python:3.11-slim
    container_name: analytics-etl-dashboard
    working_dir: /app
    volumes:
      - .:/app
    command: streamlit run dashboard/app.py --server.port 8503 --server.address 0.0.0.0
    ports:
      - "8503:8503"
